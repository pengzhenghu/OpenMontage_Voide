`python -m backlot open [project-id]` 自动检测端口（默认来自 `DEFAULT_PORT` 或 `BACKLOT_PORT` 环境变量），若后台未运行则通过 `subprocess.Popen` 以分离进程启动 `serve --port` 并轮询 `/api/health` 直到就绪；`python -m backlot serve --port N` 前台运行。演示流程：先执行 `scripts/backlot_simulate_run.py` 生成示例项目，再用 `open backlot-demo-run` 查看。