- `python scripts/backlot_screenshot_stage.py [--stage-only|--shoot-only]` 构建演示项目并启动 Backlot 服务（端口 4790）抓取文档截图到 `docs/images/backlot/`。
- `python scripts/backlot_visual_eval.py [--bless|--interactions]` 运行视觉回归，金图存放在 `internal/evals/goldens`，产物写入 `internal/evals/captures/<timestamp>/`。
- `python scripts/kling_official_animated_explainer_e2e.py --live-tts/--live-full/--live-avatar/--live-all` 需要 `KLING_API_KEY`（可选 `KLING_API_BASE_URL`、`FAL_KEY`、`OPENAI_API_KEY`），报告写入 `projects/<project>/artifacts/`。
- `python scripts/atlas_media_smoke.py --project-dir <dir> --images-only|--videos-only --allow-paid` 才会真正调用付费 Atlas Cloud。
- `./scripts/wan_disk_cleanup.sh [--models|--venv|--uv-cache|--all] [--yes]` 默认只报告占用，加 `--yes` 才删除。