新增 mocap 动作只需运行 `node mocap/add-motion.mjs <name> <cmu_id_or_url_or_local_bvh> <category> <desc>`，即可把 BVH 转换为 `clips/<name>.json` 并写入 `catalog.json`，无需修改代码；演示可通过 `npx hyperframes lint ink-theater/examples/mocap-figure` 验证。