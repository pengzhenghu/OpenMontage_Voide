Pydantic v2（`BaseModel` + `Field(default_factory=...)`）用于配置建模；`pyyaml` 解析 YAML 清单与样式；`jsonschema` 校验 pipeline manifest、checkpoint、playbook 等结构化数据；`python-dotenv` 加载 `.env`；标准库 `functools.lru_cache` 缓存 manifest 与 schema 加载结果。