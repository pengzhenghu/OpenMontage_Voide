新增样式只需在 `styles/` 下添加 `<name>.yaml`，或通过 `lib/playbook_generator.save_playbook()` 写入 `styles/custom/<name>.yaml`；两者同时存在时预设优先。运行前需确保 `schemas/styles/playbook.schema.json` 可用，否则 `validate_playbook` 会抛出文件读取异常。