默认 `pytest` 会阻断所有非回环网络，需设置环境变量 `OPENMONTAGE_ALLOW_NETWORK=1` 并显式选择标记才能运行真实 API 用例（`pytest -m live_api`）。基准评测通过 `python -m tests.eval.bench_runner --tag <tag> [--verbose]` 运行，QA 端到端脚本按 `QA_PLAN.md` 中的阶段顺序逐个执行。