文档中记录的运行期命令用于验证环境：`python -c "from tools.tool_registry import registry; ..."` 输出能力目录与提供商菜单；`uv pip install diffusers transformers accelerate torch pillow requests` 安装 Apple Silicon MPS 依赖；`export VIDEO_GEN_LOCAL_ENABLED=true` 启用本地生成；ComfyUI 通过 `COMFYUI_SERVER_URL=http://localhost:8188` 及可选的 `COMFYUI_IMAGE_SERVER_URL` / `COMFYUI_VIDEO_SERVER_URL` / `COMFYUI_MUSIC_SERVER_URL` 多实例路由。